import { musicElement } from "./musicElement";

export interface musicRecords {
    pop: musicElement,
    bonus : musicElement,
    negative : musicElement,
    plic : musicElement,
    removeLine : musicElement,
    select : musicElement,
    selectLamp : musicElement,
    pay : musicElement,
}

